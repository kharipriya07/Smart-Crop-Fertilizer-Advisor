# Server
server.port=8080

# Database - Aiven MySQL
spring.datasource.url=${DB_URL:jdbc:mysql://mysql-224c4940-anurag-9dba.l.aivencloud.com:16389/defaultdb?sslMode=REQUIRED&useSSL=true&requireSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:avnadmin}
spring.datasource.password=${DB_PASSWORD:AVNS_ODGRO_DFciPnoSW0H58}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Initialize seed data
spring.sql.init.mode=never
spring.sql.init.continue-on-error=true

# JWT
app.jwt.secret=${JWT_SECRET:GardenAdvisorSecretKey2024SuperSecureKeyForJWT}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:3000}

# Logging
logging.level.com.gardenadvisor=DEBUG
logging.level.org.springframework.security=INFO